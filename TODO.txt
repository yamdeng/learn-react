// 기본 컬럼 설정 셋팅 방법
const defaultColDef = useMemo(() => {
    return {
      editable: true,
      enableRowGroup: true,
      enablePivot: true,
      enableValue: true,
      filter: true,
      flex: 1,
      minWidth: 100,
    };
  }, []);


  #.getRowId를 활용해야하는지 확인 필요




pagination={pagination}
paginationPageSize={paginationPageSize}
paginationPageSizeSelector={paginationPageSizeSelector}
params.api.paginationGetPageSize()
params.api.paginationGetCurrentPage() : 0이 시작임

newPage = true
newPageSize = true
suppressScrollOnNewData=true